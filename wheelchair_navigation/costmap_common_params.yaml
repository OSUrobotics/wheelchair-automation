map_type: costmap
transform_tolerance: 0.2
obstacle_range: 20.0
raytrace_range: 20.0

#Origial inflation_radius
#inflation_radius: 0.2

inflation_radius: 0.7

#Adding cost_scaling_factor
cost_scaling_factor: = 4.0

obstacle_layer:
  observation_sources: base_scan

observation_sources: base_scan

base_scan: {sensor_frame: base_footprint,
            data_type: LaserScan,
            topic: /scan_multi,
            expected_update_rate: 0.2,
            observation_persistence: 0.0,
            marking: true,
            clearing: true,
            #Fixing Min Height? Not Sure if this helps
            # min_obstacle_height: -0.10,
            # max_obstacle_height: 2.0}
            min_obstacle_height: -5.10,
            max_obstacle_height: 5.0}
